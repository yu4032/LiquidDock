# Stage Manager RED checkpoint

This checkpoint is intentionally represented by executable tests in `StageWorkspacePolicyTest`; this note exists only to record that no production Stage policy has been added before the first failing CI run.
