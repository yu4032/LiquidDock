name: Apply Recents/Weather GREEN
on:
  push:
    branches: [feat/widget-dark-content-adaptation]
permissions:
  contents: write
jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/widget-dark-content-adaptation
      - name: Apply verified production patch
        run: python3 scripts/apply_recents_weather_green.py
      - name: Commit production patch
        shell: bash
        run: |
          rm -f scripts/apply_recents_weather_green.py
          rm -f .github/workflows/apply-recents-weather-green.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m 'fix: settle recents wallpaper and map weather owners'
          git push origin HEAD:feat/widget-dark-content-adaptation
