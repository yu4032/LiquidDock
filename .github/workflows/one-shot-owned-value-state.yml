name: One-shot owned value state applicator

on:
  push:
    branches:
      - refactor/pure-runtime-state-tests
    paths:
      - .github/scripts/apply_owned_value_state.py
      - .github/workflows/one-shot-owned-value-state.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/pure-runtime-state-tests
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python .github/scripts/apply_owned_value_state.py
      - run: git diff --check
      - name: Commit generated ownership wiring and remove applicator
        run: |
          rm .github/scripts/apply_owned_value_state.py
          rm .github/workflows/one-shot-owned-value-state.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add src/main/java/com/hellovoid/liquiddock/LauncherGlassVendorMaterialSuppressor.java \
                  src/main/java/com/hellovoid/liquiddock/MiuixFolderGlassHook.java \
                  .github/scripts/apply_owned_value_state.py \
                  .github/workflows/one-shot-owned-value-state.yml
          git commit -m "refactor: route material restore ownership through pure state"
          git push origin HEAD:refactor/pure-runtime-state-tests
