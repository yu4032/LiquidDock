name: API101 migration build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporarily disabled while the one-shot batched source transform commits the final change."
